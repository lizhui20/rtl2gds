# 安全说明

## 报告范围

以下问题应按安全事件处理：

- 虚拟机包含密码、Token、SSH/GPG 私钥或有效登录会话
- 镜像包含个人文件、客户设计或未公开的 PDK
- 下载文件被替换、校验不一致或疑似包含恶意软件
- 默认开放服务可被外部直接访问

## 报告方式

请优先使用 GitHub 仓库的私密安全报告功能。若该功能未启用，请先创建不含敏感细节的 Issue，请求维护者提供私密联系渠道。

不要在公开 Issue 中粘贴凭据、私钥、客户数据或可直接利用的敏感路径。维护者确认问题后，应下线受影响的网盘文件、吊销泄露凭据并发布新的校验值。

## 使用者注意事项

网盘附带指南披露了默认密码和固定 MAC，仓库不复述具体值。首次启动后立即修改密码并生成唯一 MAC；在完成安全检查前使用 NAT 或隔离网络，不要将虚拟机直接暴露到公网。维护者应尽快轮换凭据并重新发布脱敏镜像。
